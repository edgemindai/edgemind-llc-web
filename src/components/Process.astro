---
const steps = [
  {
    number: '01',
    title: 'Discover',
    description:
      'We start with a deep dive into your business — understanding your workflows, pain points, data assets, and goals to identify where AI can make the biggest impact.',
  },
  {
    number: '02',
    title: 'Design',
    description:
      'We craft a tailored AI roadmap with clear milestones, expected ROI, and a realistic timeline. No black boxes — you understand exactly what we\'re building and why.',
  },
  {
    number: '03',
    title: 'Deliver',
    description:
      'Our team builds, tests, and deploys your AI solution with minimal disruption to your operations. We integrate with your existing systems and train your team.',
  },
  {
    number: '04',
    title: 'Optimize',
    description:
      'AI gets better with time. We monitor performance, refine models, and continuously optimize to ensure you get increasing returns on your investment.',
  },
];
---

<section id="process" class="process-section">
  <div class="container">
    <div class="section-header" style="text-align: center;">
      <span class="section-label">How We Work</span>
      <h2>A proven process to<br />deliver real results</h2>
      <p style="margin: 1rem auto 0;">
        Our structured approach minimizes risk and maximizes value at every stage.
      </p>
    </div>

    <div class="process-grid">
      {steps.map((step, i) => (
        <div class={`process-step animate animate-delay-${i + 1}`}>
          <div class="step-number">{step.number}</div>
          <h3>{step.title}</h3>
          <p>{step.description}</p>
          {i < steps.length - 1 && <div class="step-connector" />}
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .process-section {
    background: var(--color-bg-alt);
  }

  .process-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    position: relative;
  }

  .process-step {
    position: relative;
    padding: 2rem 1.5rem;
    text-align: center;
  }

  .step-number {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 56px;
    height: 56px;
    border-radius: 50%;
    background: var(--color-accent-glow);
    border: 2px solid var(--color-accent);
    color: var(--color-accent-light);
    font-weight: 800;
    font-size: 1.1rem;
    margin-bottom: 1.25rem;
  }

  .process-step h3 {
    margin-bottom: 0.75rem;
  }

  .process-step p {
    font-size: 0.9rem;
    margin: 0 auto;
  }

  .step-connector {
    display: none;
  }

  @media (min-width: 769px) {
    .step-connector {
      display: block;
      position: absolute;
      top: 2rem;
      right: -1rem;
      width: 2rem;
      height: 56px;
      display: flex;
      align-items: center;
    }

    .step-connector::after {
      content: '';
      width: 100%;
      height: 2px;
      background: var(--color-border);
      display: block;
    }
  }

  @media (max-width: 1024px) {
    .process-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .step-connector {
      display: none;
    }
  }

  @media (max-width: 640px) {
    .process-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
